# afterburn writes cloud-provided keys to ~/.ssh/authorized_keys.d/afterburn
# (it does not sync a combined authorized_keys file; FCOS reads the directory
# via ssh-key-dir — we point stock sshd at it instead).
AuthorizedKeysFile .ssh/authorized_keys .ssh/authorized_keys.d/afterburn
